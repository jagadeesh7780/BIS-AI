# BIS AI V2 — Frontend Environment Variables
# STEP 13 FIX: Groq API key REMOVED — API keys must never go in the frontend.
# All LLM calls are made server-side only via FastAPI backend.

VITE_API_URL=http://localhost:8000
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_key_here
